<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <activity
            android:name=".LauncherActivity"
            android:exported="true"
            android:label="Android 布局实验"
            android:theme="@style/Theme.MyApplication"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LinearActivity" android:label="线性布局" />
        <activity android:name=".TableActivity" android:label="表格布局" />
        <activity android:name=".Constraint1Activity" android:label="约束布局1" />
        <activity android:name=".Constraint2Activity" android:label="约束布局2" />
        <activity android:name=".ComposeTaskActivity" android:label="Compose 任务列表" />

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name" />

    </application>

</manifest>
